--Instalar ORM Prisma 
npm i prisma -D

--Instalar dotenv
npm install dotenv

--Inizializar con SQLite 
 npx prisma init --datasource-provider sqlite

 --Despues de crear la base de datos hacer esto
 npx prisma migrate dev --name init